{
  "name": "@sixsentences/browser-extension",
  "version": "0.2.0-alpha.1",
  "private": true,
  "type": "module",
  "description": "Self-hostable Manifest V3 capture client for SixSentences",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs --out build/unpacked",
    "test": "node --test tests/*.test.mjs"
  }
}
