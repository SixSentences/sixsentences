/**
 * Launch controls for product surfaces that remain visible while their
 * workflows are still being validated. Keep the UI fail-closed: disabled
 * features must not mount their interactive entry points.
 */
export const REPOSITORY_VISUAL_SOURCE_ENABLED: boolean = false;
